# ======================================
# Frontend environment template
# Copy this file to frontend/.env
# ======================================

# Fallback checkout config
VITE_CURRENCY=COP # Use the same currency as the backend
VITE_BASE_FEE=2500  # Use the same base fee as the backend
VITE_DELIVERY_FEE=5000 # Use the same delivery fee as the backend

